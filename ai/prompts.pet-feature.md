# Prompt

Feature's name: pet
Data structure: #pet.model.ts
API endpoints:

- /api/pets - HTTP methods: GET
- /api/pets/[id]?isURL=true - HTTP methods: GET

use cases:
usePetByUrl (by url)
usePet
usePets

components:
PetPreview
Pets using PetPreview
Pet (to show all pet details, pet has to be identified by its URL)
